# Conventions

Commands are intended to be runnable unless labeled as pseudocode.

Placeholders use uppercase names:

```bash
export API_TOKEN="REPLACE_ME"
```

Standard callouts are defined in the editorial style guide.
